server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/mensao/microservice-config-repo.git
          search-paths:
            - foo   # foo路径
            - bar   # bar路径